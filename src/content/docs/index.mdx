---
title: zkmcu
description: "no_std Rust ZK verifiers for microcontrollers. Current focus: post-quantum Semaphore-style identity proofs with dual-hash STARK composition on a $7 RP2350, plus the existing BN254 Groth16, BLS12-381 Groth16, and winterfell STARK family in under 128 KB SRAM."
hero:
  tagline: |
    no_std Rust ZK verifiers for microcontrollers. Current focus: a post-quantum Semaphore-style identity proof, verified on a $7 Pico 2 W in 1.6 s under dual-hash (Poseidon2 + Blake3) STARK composition. The earlier BN254 / BLS12 / winterfell-STARK family still ships under 128 KB SRAM. Ofcourse nobody had published this before, so I had to find out myself.
  actions:
    - text: Read the writeup
      link: /research/2026-04-30-pq-semaphore-128bit/
      icon: right-arrow
      variant: primary
    - text: Getting started
      link: /getting-started/
      icon: open-book
      variant: secondary
    - text: View on GitHub
      link: https://github.com/Niek-Kamer/zkmcu
      icon: external
      variant: minimal
---

import { Card, CardGrid, Code, Aside } from '@astrojs/starlight/components';
import HeroBadges from '../../components/HeroBadges.astro';
import HeroStat from '../../components/HeroStat.astro';
import { usAt } from '../../lib/benchmarks-index';
import { fmtMs } from '../../lib/format';

<HeroBadges badges={[
  { text: 'no_std Rust', variant: 'note' },
  { text: 'post-quantum, dual-hash', variant: 'success' },
  { text: '128 KB SRAM tier', variant: 'tip' },
]} />

## In five lines

Yeah so the API is pretty much three crates, one function each. Pull in wichever one you need, hand it bytes, check the `bool`.

```rust
use zkmcu_verifier::verify_bytes;                   // BN254 / EIP-197
// or
use zkmcu_verifier_bls12::verify_bytes;             // BLS12-381 / EIP-2537
// or
use zkmcu_verifier_stark::fibonacci::verify;        // winterfell STARK, Fibonacci AIR

let ok = verify_bytes(&vk_bytes, &proof_bytes, &public_bytes)?;
```

All three: `no_std`, pure Rust, one function call. Every number on this page comes from a real bench on a real RP2350, not a simulator.

## Measured on silicon

<HeroStat
  value={`${fmtMs(usAt('pq_sem_m33_pe1'))} ms`}
  label="Dual-hash PQ-Semaphore depth-10 verify, Cortex-M33"
  href="/research/2026-04-30-pq-semaphore-128bit/"
>
  Two independent FRI proofs over the same statement: Poseidon2-`BabyBear`-16 (audited round constants) and Blake3 1.8. Both must accept. Soundness composes across the two hash families, so a cryptanalytic surprise on either doesn't collapse the verifier. Hazard3 RV32 lands at {fmtMs(usAt('pq_sem_rv32_pe1'))} ms on the same die.
</HeroStat>

<CardGrid>
  <Card title={`${fmtMs(usAt('bn254_m33_semaphore'))} ms BN254 Semaphore`} icon="approve-check-circle">
    **Classical baseline.** Real Ethereum-Semaphore v4 depth-10 Groth16 proof, the same VK that the `0x08` precompile accepts. Verifies on the same $7 MCU. [More](/semaphore/)
  </Card>
  <Card title={`${fmtMs(usAt('threshold_m33_prove'))} ms STARK prove`} icon="rocket">
    **First STARK prover on bare metal.** Threshold-check circuit, prove `value < threshold`, N=64, BabyBear+Quartic, 64 KB heap peak. FibRace needs 3 GB for the same predicate. [More](/stark-prover/)
  </Card>
  <Card title={`${fmtMs(usAt('stark_m33_tlsf'))} ms STARK verify`} icon="puzzle">
    **Deterministic to silicon noise floor.** Winterfell Fibonacci AIR over Goldilocks, quadratic extension, TlsfHeap allocator. Variance sits at 0.08 %. [More](/stark/)
  </Card>
  <Card title={`${fmtMs(usAt('bls12_m33'))} ms BLS12-381`} icon="document">
    Sibling Groth16 verifier on the same $7 silicon. First public `no_std` BLS12-381 verifier on Cortex-M, as far as I can find. If anyone knows earlier work, tell me and I'll update.
  </Card>
</CardGrid>

<Aside type="note" title="Why this even exists">
  Every SNARK and STARK verifier out there ships for phones or servers. Hardware wallets, offline credential readers, IoT tags, none of them verify ZK proofs on-device. Not because it was impossible, just because nobody had shipped an open-source `no_std` verifier to try it with.

  So I figured someone had to actually benchmark wether it fits, and here we are. All three dominant proof-system families, one 128 KB SRAM budget: Ethereum's BN254 precompile format, BLS12-381 for EIP-2537, and winterfell's Goldilocks-based STARK.
</Aside>

## What it's for

<CardGrid>
  <Card title="Hardware wallets" icon="laptop">
    ZK verify on the secure element itself, so a compromised phone can't feed the wallet a bogus Semaphore or Tornado action and get rubber-stamped. Under TlsfHeap the timing is deterministic to silicon noise floor, so side-channel resistance is mostly free.
  </Card>
  <Card title="Offline credentials" icon="seti:lock">
    Transit turnstiles, festivals, borders. Verify a privacy-preserving credential with no network, no server round-trip. Groth16 and STARK both work, pick wichever your issuer uses.
  </Card>
  <Card title="IoT attestation" icon="linux">
    Small devices checking each other's SNARK-attested provenance without a cloud relay in the middle. At 75 ms per STARK, you can actually verify per-packet without falling over.
  </Card>
  <Card title="Cross-chain / zkVM proofs" icon="external">
    BLS12-381 sync-committee proofs, Filecoin PoSt, Aleo, winterfell / Miden VM traces. If it ships on BN254, BLS12-381, or Goldilocks STARK, the verifier fits on your MCU.
  </Card>
</CardGrid>

## What makes it a family

<CardGrid>
  <Card title="Same repo shape" icon="open-book">
    Three crates, one per proof system, all with the same API shape. Same test-vector loader, same firmware bench template. Write against `zkmcu_verifier::verify_bytes` today, swap to BLS12 or STARK tomorrow without rewriting your embedded code.
  </Card>
  <Card title="Same 128 KB silicon tier" icon="document">
    BN254 Groth16 uses 97 KB RAM, BLS12-381 uses 116 KB, STARK uses 100 KB. All measured on the same RP2350 Cortex-M33 at 150 MHz. Any hardware-wallet-class chip (nRF52832, STM32F405, Ledger ST33, Infineon SLE78) runs any of the three.
  </Card>
  <Card title="Deterministic timing" icon="approve-check-circle">
    With the production TlsfHeap allocator, STARK verify variance hits **0.08 %**, wich is the silicon noise floor. That's timing-side-channel resistance without writing constant-time code by hand, wich is nice because constant-time crypto is a pain in the ass to review. [How](/determinism/)
  </Card>
  <Card title="Two ISAs, one source" icon="rocket">
    ARM Cortex-M33 and RISC-V Hazard3 running the exact same crypto source, same chip. Only the linker script and the cycle-counter read differ. Measured cross-ISA ratios: 1.21× for STARK, 1.33× for BN254 Groth16, 2.56× for BLS12-381. M33 wins every time, but the gap depends wildly on workload.
  </Card>
</CardGrid>

## Where to next

<CardGrid>
  <Card title="PQ-Semaphore writeup" icon="rocket">
    The six-phase 128-bit security build. Each phase tested one knob, predicted the cost, then either confirmed or rejected the hypothesis on real silicon. One phase rejected its own hypothesis; the final phase landed exactly on its predicted floor. [Read it](/research/2026-04-30-pq-semaphore-128bit/)
  </Card>
  <Card title="Semaphore real-world proof" icon="approve-check-circle">
    Production Ethereum-Semaphore VK, snarkjs-generated proof, verified on a $7 MCU in 1.18 s. No simplifications. [See it](/semaphore/)
  </Card>
  <Card title="Deterministic timing" icon="pencil">
    How picking the right allocator knocked timing variance from 0.45 % to 0.08 %. Matters a lot if you care about side channels. [The methodology](/determinism/)
  </Card>
  <Card title="Benchmarks" icon="bars">
    Numbers straight from the silicon. Four verifier families, both cores, three allocators. [Full data](/benchmarks/)
  </Card>
</CardGrid>
